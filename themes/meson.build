pkgdatadir = join_paths(get_option('prefix'), get_option('datadir'))
moduledir = join_paths(pkgdatadir, 'rthemes')

themes = [
  'adwaita.rtheme',
  'solarized.rtheme',
  'class.rtheme'
  'risi.rtheme'
]

install_data(themes, install_dir: moduledir)